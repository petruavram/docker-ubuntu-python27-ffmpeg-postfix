# docker-python27-ffmpeg-postfix

A docker file for generating an container based on Ubuntu:xenial with the default 
instalation of Python 2.7, ffmpeg and postfix.


### Build

`docker build -t <imageName> .`
